#version 450

layout (local_size_x = 256) in;

const uint count = 2;

layout(binding = 0, std430) buffer world_in{
  vec2 pos[count];
  vec2 vel[count];
};

void main() {
	uint id = gl_GlobalInvocationID.x;
  if(id < count){
    pos[id] = vel[id] + pos[id];
  }
  if(id == 0){
    pos[0] = vec2(0.3, 0.4) + pos[0];
  }
}